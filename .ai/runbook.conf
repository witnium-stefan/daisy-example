# sourced by ~/.claude/runbook/runbook.sh and lib/codex-run.sh
# daisy-example: Opsie Daisy's synthetic example application (spec: opsie-daisy docs/architecture/example-application.md).
# Land = merge into main + push. Node 24 via fnm, as in opsie-daisy.
LAND_TARGET=main
FULL_GATES='fnm exec --using 24 -- npm test'
BOOTSTRAP='fnm exec --using 24 -- npm ci'
