---
id: 0001
title: repository and CI: tiny sources, digest-pinned GHCR images, and a Compose artifact the Opsie Daisy intake accepts
level: 2
base: 
stream: 
kind: feature
design_review: 0
code_review: 0
time_budget_min: 60
time_budget_why: 
process_version: 2026-09-07
missed: 
epic: 
state: failed
asked: 2026-09-09T17:08:06Z
started: 2026-09-09T17:08:06Z
last_started: 2026-09-09T17:09:23Z
resumed: 1
---
## Ask
This is the synthetic example application for Opsie Daisy; the reviewed specification is /Users/stefan/Projects/Witnium/opsie-daisy-repo/docs/architecture/example-application.md (read it first: sections 'Aspect → responsibility → observable evidence', 'State and restore oracle', 'Planned repository and current intake', and slice 1 'Repository and CI'). Outcome of this slice: (1) the repository layout the spec plans: services web, api and worker as tiny Node 24 programs with no dependencies beyond the standard library where possible (a database client for api/worker is acceptable and must be at least 14 days old — the workstation's supply-chain rule), each with a Dockerfile pinned by base-image digest, health and version endpoints as the spec's aspects table requires, and a docker-compose.yaml in the exact shape the spec shows (services web, api, worker, postgres with images by digest on ghcr.io/witnium; the postgres image mirrored by digest, not pulled from Docker Hub at deploy time); (2) GitHub Actions CI that builds the three images and publishes them to ghcr.io/witnium/daisy-example-{web,api,worker} using the repository secret GHCR_PUBLISH_TOKEN (username witnium; the token is available only as a secret, never echoed), VERIFIES package visibility instead of setting it (decision, PM correction: GitHub's Packages API cannot change visibility; the owner sets it once in the package settings): after publishing, CI fetches each image manifest anonymously and asserts web, api and postgres answer 200 while worker answers 401/403; a mismatch fails the workflow with a message naming the package and the settings page, so the one-time owner action is loud, never silent; mirrors the pinned postgres image to ghcr.io/witnium/daisy-example-postgres, and writes the resulting digests into a generated compose artifact (docker-compose.yaml with @sha256 digests, committed back or published as a release asset — choose the one the spec's collectors accept and say which); (3) npm test runs the services' unit tests locally without Docker (health and version handlers, the ledger write path against an in-memory stand-in is acceptable for this slice) and validates the compose artifact's shape (GHCR-only, digest-pinned, no tag+digest, the exact keys the intake accepts); (4) README documents purpose, the aspects, and how to publish. BOUNDARIES are behavioral: no failure switches, no campaign yet (later slices); no secrets in files or logs; no live pushes from tests; list touched files in the summary. American spelling.

## Check
fnm exec --using 24 -- npm test

## Acceptance
- A1. The check succeeds: fnm exec --using 24 -- npm test
- probe: fnm exec --using 24 -- node --experimental-strip-types services/api/src/server.ts --version 2>&1 | tail -2 || fnm exec --using 24 -- npm test 2>&1 | tail -4
