---
id: 0003
title: services and state: web, api and worker flow with real PostgreSQL commits, files on a volume, configuration and secret consumption, health and version endpoints, and the ledger oracle
level: 2
base: 
stream: 
kind: feature
design_review: 0
code_review: 0
time_budget_min: 60
time_budget_why: 
process_version: 2026-09-07
missed: 
epic: 
state: done
asked: 2026-09-09T17:25:00Z
started: 2026-09-09T17:25:01Z
last_started: 2026-09-09T17:25:01Z
landed: 2026-09-09T17:36:22Z
verified: 2026-09-09T17:36:23Z
---
## Ask
Specification: /Users/stefan/Projects/Witnium/opsie-daisy-repo/docs/architecture/example-application.md (read sections 'Aspect → responsibility → observable evidence', 'State and restore oracle' and slice 2 'Services and state'). Slice 1 (item 0001, commit 3cea9d9) left services/{web,api,worker}/main.mjs, services/{common,database}.mjs, services/worker/ledger.mjs (ledger tested in memory), fixtures/ledger.json, the compose generator and 36 tests. Outcome of this slice, exactly as the spec's aspects table demands: (1) the durable flow: the worker appends monotonic ledger entries to PostgreSQL (pg@8.16.3 already pinned) with acknowledged commits; the api reads the ledger and exposes it; a second connection sees committed rows; a restart retains them; the web front renders the api's answer; (2) files: the api writes a file per ledger batch to a mounted volume path from configuration and lists them, so a persistent volume is exercised; (3) configuration and secret: a nonsecret EXAMPLE_MESSAGE binding changes a visible response; a secret EXAMPLE_TOKEN is required by the worker to talk to the api and never appears in any response, log or error — a test proves it; missing configuration is an explicit startup refusal naming the key (no defaults, per the workstation rule); (4) health and version endpoints on every service as the spec requires (ready only when the dependency it needs answers), plus the internal management port 9090 decided in slice 1; (5) the restore oracle: an endpoint or script that, given a ledger prefix, verifies a restored copy shows exactly that complete prefix (the spec's 'complete-prefix restore proof'); (6) tests run without Docker by default (in-memory stand-in for PostgreSQL where already present) and, when a DATABASE_URL to a disposable PostgreSQL is provided, run the real-commit tests against it — never a default URL; (7) README updated; the compose artifact still validates. BOUNDARIES are behavioral: no failure switches, no campaign (later slices); no new dependencies beyond pg; no secrets in files, logs or responses; list touched files in the summary. American spelling.

## Check
fnm exec --using 24 -- npm test

## Acceptance
- A1. probe: fnm exec --using 24 -- npm test 2>&1 | tail -5
